[project]
name = "nautilus"
version = "0.2.0"
description = "Terminal chat interface for Nemotron-3-Super running locally via TensorRT-LLM on DGX Spark"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0",
    "rich>=14.0.0",
]
