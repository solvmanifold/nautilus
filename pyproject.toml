[project]
name = "nemo"
version = "0.1.0"
description = "Terminal chat interface for Nemotron 3 Nano running locally via MLX on Apple Silicon"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mlx-lm>=0.30.7",
    "rich>=14.0.0",
]
